import type { DateTime } from '@gravity-ui/date-utils';
import type { Preset } from './defaultPresets';
export declare function getPresetTitle(start: string, end: string, presets?: Preset[]): string;
export declare function filterPresets(presets: Preset[], minValue?: DateTime): Preset[];
export interface PresetTab {
    id: string;
    title: string;
    presets: Preset[];
}
export declare function getDefaultPresetTabs({ withTime, minValue, }: {
    minValue?: DateTime;
    withTime?: boolean;
}): PresetTab[];
export declare function filterPresetTabs(tabs: PresetTab[], { minValue }?: {
    minValue?: DateTime;
}): PresetTab[];
