export interface Preset {
    from: string;
    to: string;
    title: string;
}
export declare const DEFAULT_DATE_PRESETS: Preset[];
export declare const DEFAULT_TIME_PRESETS: Preset[];
export declare const DEFAULT_OTHERS_PRESETS: Preset[];
export declare const allPresets: Preset[];
