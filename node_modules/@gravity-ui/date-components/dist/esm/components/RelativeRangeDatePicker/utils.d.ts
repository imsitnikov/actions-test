import type { Value } from '../RelativeDatePicker';
import type { RangeValue } from '../types';
import type { Preset } from './components/Presets/defaultPresets';
export declare function resolveTimeZone(timeZone: string): string;
export declare function normalizeTimeZone(timeZone: string): string;
export declare function getTimeZoneOffset(timeZone: string): string;
interface GetDefaultTitleArgs {
    value: RangeValue<Value | null> | null;
    timeZone: string;
    alwaysShowAsAbsolute?: boolean;
    format?: string;
    presets?: Preset[];
}
export declare function getDefaultTitle({ value, timeZone, alwaysShowAsAbsolute, format, presets, }: GetDefaultTitleArgs): string;
export {};
