"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useOnIntersected = void 0;
const react_1 = __importDefault(require("react"));
const useOnIntersected = (element, onIntersected, options) => {
    react_1.default.useEffect(() => {
        const observer = new IntersectionObserver(([entry]) => {
            if (entry.isIntersecting) {
                onIntersected();
            }
        }, options);
        if (element) {
            observer.observe(element);
        }
        return () => (element === null ? undefined : observer.unobserve(element));
    }, [element, onIntersected, options]);
};
exports.useOnIntersected = useOnIntersected;
