"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useSyncWithLS = void 0;
const react_1 = __importDefault(require("react"));
const useSyncWithLS = ({ dataSourceName = 'sync-tabs', callback, uniqueKey, }) => {
    const key = `${dataSourceName}.${uniqueKey}`;
    const handler = (event) => {
        if (event.key === key && event.newValue) {
            return callback();
        }
        return undefined;
    };
    react_1.default.useEffect(() => {
        // Action in non-active tab
        window.addEventListener('storage', handler);
        return () => {
            window.removeEventListener('storage', handler);
            localStorage.removeItem(key);
        };
    });
    return {
        callback: react_1.default.useCallback(() => {
            localStorage.setItem(key, String(Number(localStorage.getItem(key) || '0') + 1));
            return callback();
        }, [key, callback]),
    };
};
exports.useSyncWithLS = useSyncWithLS;
