import React from 'react';
import type { StoriesItemMedia } from '../../types';
export interface MediaRendererProps {
    media: StoriesItemMedia;
}
export declare function MediaRenderer({ media }: MediaRendererProps): React.JSX.Element;
