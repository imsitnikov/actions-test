import React from 'react';
import type { ModalCloseReason } from '@gravity-ui/uikit';
import type { StoriesGroupItem } from './types';
import './StoriesGroup.css';
export interface StoriesGroupProps {
    open: boolean;
    groups: StoriesGroupItem[];
    initialStoryIndex?: [groupIndex: number, itemIndex: number];
    disableOutsideClick?: boolean;
    maxSliderItemsCount?: number;
    onClose?: (event: MouseEvent | KeyboardEvent | React.MouseEvent<HTMLElement, MouseEvent>, reason: ModalCloseReason | 'closeButtonClick') => void;
    onItemSelect?: (itemIndexes: [groupIndex: number, itemIndex: number], selectedFromThumbnail: boolean) => void;
}
export declare const StoriesGroup: ({ open, groups, onItemSelect, disableOutsideClick, initialStoryIndex, maxSliderItemsCount, onClose, }: StoriesGroupProps) => React.JSX.Element | null;
