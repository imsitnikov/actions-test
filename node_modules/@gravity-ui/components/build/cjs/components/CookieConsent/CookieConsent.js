"use strict";
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CookieConsent = void 0;
const react_1 = __importDefault(require("react"));
const cn_1 = require("../utils/cn");
const ConsentNotification_1 = require("./components/ConsentNotification/ConsentNotification");
const ConsentPopup_1 = require("./components/ConsentPopup/ConsentPopup");
const types_1 = require("./components/ConsentPopup/types");
const SimpleConsent_1 = require("./components/SimpleConsent/SimpleConsent");
const b = (0, cn_1.block)('analytics');
const CookieConsent = (_a) => {
    var { consentManager, onConsentPopupClose, manageCookies } = _a, popupProps = __rest(_a, ["consentManager", "onConsentPopupClose", "manageCookies"]);
    const [isOpened, setIsOpened] = react_1.default.useState(false);
    react_1.default.useEffect(() => {
        // Show banner after some timeout so that the user has time to see the service content
        const timeoutId = setTimeout(() => {
            if (consentManager.isConsentNotDefined()) {
                setIsOpened(true);
            }
        }, 1000);
        return () => clearTimeout(timeoutId);
    }, [consentManager]);
    const onConsentPopupAction = (values) => {
        consentManager.setConsents(values);
        setIsOpened(false);
        onConsentPopupClose === null || onConsentPopupClose === void 0 ? void 0 : onConsentPopupClose();
    };
    const onClose = () => {
        setIsOpened(false);
        onConsentPopupClose === null || onConsentPopupClose === void 0 ? void 0 : onConsentPopupClose();
    };
    const view = manageCookies ? 'manage' : consentManager.mode;
    if (isOpened || manageCookies) {
        switch (view) {
            case 'manage':
                return (react_1.default.createElement(ConsentPopup_1.ConsentPopup, Object.assign({}, popupProps, { className: b(), step: manageCookies ? types_1.ConsentPopupStep.Manage : types_1.ConsentPopupStep.Main, onAction: onConsentPopupAction, onClose: onClose, consentManager: consentManager })));
            case 'notification':
                return (react_1.default.createElement(ConsentNotification_1.ConsentNotification, Object.assign({}, popupProps, { className: b(), onAction: onConsentPopupAction, consentManager: consentManager })));
            case 'base':
                return (react_1.default.createElement(SimpleConsent_1.SimpleConsent, Object.assign({}, popupProps, { className: b(), onAction: onConsentPopupAction, consentManager: consentManager })));
        }
    }
    return null;
};
exports.CookieConsent = CookieConsent;
