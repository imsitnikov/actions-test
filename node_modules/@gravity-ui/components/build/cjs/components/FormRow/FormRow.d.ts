import React from 'react';
import { FormRowProps } from './types';
import './FormRow.css';
export declare const FormRow: {
    ({ className, label, labelHelpPopover, fieldId, required, children, direction, }: FormRowProps): React.JSX.Element;
    displayName: string;
} & {
    FieldDescription: {
        ({ children, className, ...elementProps }: import("./types").FormRowFieldDescriptionProps): React.JSX.Element;
        displayName: string;
    };
};
