import React from 'react';
import { Xmark } from '@gravity-ui/icons';
import { Button, Icon, Text } from '@gravity-ui/uikit';
import { block } from '../utils/cn';
import { CollapseActions } from './components/CollapseActions';
import './ActionsPanel.css';
const b = block('actions-panel');
export const ActionsPanel = ({ className, actions, onClose, renderNote }) => {
    return (React.createElement("div", { className: b(null, className) },
        typeof renderNote === 'function' && (React.createElement(Text, { className: b('note-wrapper'), as: "div", color: "light-primary", variant: "subheader-2", ellipsis: true }, renderNote())),
        React.createElement(CollapseActions, { actions: actions }),
        typeof onClose === 'function' && (React.createElement("div", { className: b('button-close-wrapper') },
            React.createElement(Button, { view: "flat-contrast", size: "m", onClick: onClose },
                React.createElement(Icon, { key: "icon", data: Xmark }))))));
};
