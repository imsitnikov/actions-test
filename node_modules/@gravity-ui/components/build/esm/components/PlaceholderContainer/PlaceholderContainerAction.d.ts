import React from 'react';
import { ButtonSize, ButtonView } from '@gravity-ui/uikit';
import './PlaceholderContainer.css';
export interface Action {
    text: string;
    loading?: boolean;
    disabled?: boolean;
    view?: ButtonView;
    size?: ButtonSize;
    handler?: (event: React.MouseEvent<HTMLButtonElement>) => void;
    href?: string;
}
interface PlaceholderContainerActionProps {
    action: Action;
}
export declare const PlaceholderContainerAction: ({ action }: PlaceholderContainerActionProps) => React.JSX.Element;
export {};
