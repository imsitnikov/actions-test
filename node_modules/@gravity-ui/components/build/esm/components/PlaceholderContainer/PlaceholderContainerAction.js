import React from 'react';
import { Button } from '@gravity-ui/uikit';
import { block } from '../utils/cn';
import { componentClassName } from './constants';
import './PlaceholderContainer.css';
const b = block(componentClassName);
export const PlaceholderContainerAction = ({ action }) => {
    const { text, handler, loading, disabled, view = 'normal', size = 'm', href } = action;
    return (React.createElement("div", { className: b('action') },
        React.createElement(Button, { className: b('action-btn'), view: view, size: size, loading: loading, disabled: disabled, onClick: handler, href: href }, text)));
};
