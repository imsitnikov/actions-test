import React from 'react';
import { ActionTooltip, Button, Icon } from '@gravity-ui/uikit';
import { block } from '../utils/cn';
import './Notification.css';
const b = block('notification');
export const NotificationAction = React.memo(function NotificationAction({ action }) {
    var _a;
    const content = renderContent(action);
    const button = (React.createElement(Button, { className: b('action', { icon: Boolean(action.icon) }), view: (_a = action.view) !== null && _a !== void 0 ? _a : 'flat', href: action.href, target: action.target, onClick: action.onClick }, content));
    return action.icon ? React.createElement(ActionTooltip, { title: action.text }, button) : button;
});
function renderContent(action) {
    return action.icon ? React.createElement(Icon, { data: action.icon }) : action.text;
}
