import React from 'react';
import { Icon } from '@gravity-ui/uikit';
import { block } from '../utils/cn';
import './Notification.css';
const b = block('notification');
export const NotificationSwipeAction = React.memo(function NotificationSwipeAction(props) {
    const { icon, text, theme = 'base', action } = props;
    return (
    // eslint-disable-next-line jsx-a11y/click-events-have-key-events, jsx-a11y/no-static-element-interactions
    React.createElement("div", { className: b('swipe-action', { theme }), onClick: action },
        React.createElement("span", { className: b('swipe-action-icon') },
            React.createElement(Icon, { data: icon, size: 16 })),
        React.createElement("span", { className: b('swipe-action-text') }, text)));
});
