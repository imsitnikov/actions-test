import React from 'react';
import { block } from '../utils/cn';
import { NotificationWrapper } from './NotificationWrapper';
import './Notifications.css';
const b = block('notifications');
export const NotificationsList = React.memo(function NotificationsList(props) {
    return (React.createElement("div", { className: b('list') }, props.notifications.map((notification) => (React.createElement(NotificationWrapper, { notification: notification, swipeThreshold: props.swipeThreshold, key: notification.id })))));
});
