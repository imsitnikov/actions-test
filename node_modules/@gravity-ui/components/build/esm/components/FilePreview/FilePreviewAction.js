import React from 'react';
import { ActionTooltip, Button, Icon } from '@gravity-ui/uikit';
export function FilePreviewAction({ id, icon, title, href, disabled, onClick, extraProps, }) {
    return (React.createElement(ActionTooltip, { id: id, title: title },
        React.createElement(Button, { onClick: onClick, view: "raised", pin: "circle-circle", href: href, disabled: disabled, size: "s", extraProps: Object.assign({ 'aria-label': title, 'aria-describedby': id }, extraProps) },
            React.createElement(Icon, { data: icon, size: 14 }))));
}
FilePreviewAction.displayName = 'FilePreviewAction';
