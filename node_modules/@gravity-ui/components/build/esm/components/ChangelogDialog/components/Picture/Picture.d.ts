import React from 'react';
import './Picture.css';
export interface PictureProps {
    className?: string;
    src: string;
    alt?: string;
    ratio?: number;
}
export declare function Picture({ className, src, alt, ratio }: PictureProps): React.JSX.Element | null;
