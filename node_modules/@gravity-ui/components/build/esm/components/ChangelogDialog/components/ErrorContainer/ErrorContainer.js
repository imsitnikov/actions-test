import React from 'react';
import { Alert } from '@gravity-ui/uikit';
import { i18n } from '../../i18n';
export function ErrorContainer({ onRetryClick, error }) {
    const { title, description } = React.useMemo(() => {
        return error && typeof error === 'object' ? error : {};
    }, [error]);
    return (React.createElement(Alert, { title: title || i18n('label_error-title'), message: description, theme: 'danger', actions: onRetryClick && [
            {
                text: i18n('button_retry'),
                handler: onRetryClick,
            },
        ] }));
}
