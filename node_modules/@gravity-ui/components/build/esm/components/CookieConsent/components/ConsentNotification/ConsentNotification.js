import React from 'react';
import { Alert, Button, Link, useMobile } from '@gravity-ui/uikit';
import { block } from '../../../utils/cn';
import { i18n } from '../../i18n';
import './ConsentNotification.css';
const b = block('consent-notification');
export const ConsentNotification = ({ policyLink, onAction, className, policyLinkText = i18n('label_policy'), text = i18n('label_text'), buttonOkText = i18n('button_OK'), }) => {
    const mobile = useMobile();
    const onClick = () => {
        onAction('All');
    };
    const message = (React.createElement(React.Fragment, null,
        React.createElement("span", { className: b('text') }, text),
        policyLink ? (React.createElement("span", { className: b('text') },
            i18n('details_text'),
            ' ',
            React.createElement(Link, { href: policyLink, target: "_blank" }, policyLinkText),
            ".")) : null));
    const actions = (React.createElement(Button, { className: b('button'), onClick: onClick, size: "l", view: "action" }, buttonOkText));
    return (React.createElement(Alert, { className: b({ type: mobile ? 'mobile' : 'default' }, className), theme: "normal", view: "filled", message: message, actions: actions }));
};
