import React from 'react';
import { Button, Portal } from '@gravity-ui/uikit';
import { block } from '../../../utils/cn';
import { i18n } from '../../i18n';
import './SimpleConsent.css';
const b = block('simple-consent');
const buttons = ['decline', 'accept'];
export const SimpleConsent = (props) => {
    const { className, text = i18n('label_text'), buttonAcceptText = i18n('button_accept'), buttonDeclineText = i18n('button_decline'), onAction, } = props;
    const onClick = (isAll) => {
        return () => {
            onAction(isAll ? 'All' : 'OnlyNecessary');
        };
    };
    return (React.createElement(Portal, null,
        React.createElement("div", { className: b(null, className) },
            React.createElement("div", { className: b('container') },
                React.createElement("span", { className: b('text') }, text),
                React.createElement("div", { className: b('buttons') }, buttons.map((button) => (React.createElement(Button, { key: button, className: b('button'), onClick: onClick(button === 'accept'), size: "xl", view: button === 'accept' ? 'action' : 'outlined' }, button === 'accept' ? buttonAcceptText : buttonDeclineText))))))));
};
