interface FindNextItemsProps {
    list: string[];
    index: number;
    step: number;
    disabledItems?: Record<string, boolean>;
}
export declare const findNextIndex: ({ list, index, step, disabledItems }: FindNextItemsProps) => number | undefined;
export {};
