import type { InitialListParsedState, ListItemId, ListItemType, ParsedState } from '../types';
type ListParsedStateResult<T> = ParsedState<T> & {
    initialState: InitialListParsedState;
};
export declare function getListParsedState<T>(items: ListItemType<T>[], 
/**
 * For example T is entity type with id what represents db id
 * So now you can use it id as a list item id in internal state
 */
getItemId?: (item: T) => ListItemId): ListParsedStateResult<T>;
export {};
