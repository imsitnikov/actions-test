import React from 'react';
import type { ListState } from '../types';
interface UseListStateProps {
    /**
     * Initial state values
     */
    initialValues?: Partial<ListState>;
    /**
     * Ability to pass link to another state value
     *
     * ```tsx
     * const listState = useListState()
     *
     * // inside your component
     * const innerListState = useListState({
     *  controlledValues: listState
     * })
     * ```
     */
    controlledValues?: Partial<ListState>;
}
export declare const useListState: ({ initialValues, controlledValues }?: UseListStateProps) => {
    disabledById: Record<string, boolean>;
    setDisabled: React.Dispatch<React.SetStateAction<Record<string, boolean>>>;
    selectedById: Record<string, boolean>;
    setSelected: React.Dispatch<React.SetStateAction<Record<string, boolean>>>;
    expandedById: Record<string, boolean>;
    setExpanded: React.Dispatch<React.SetStateAction<Record<string, boolean>>>;
    activeItemId: string | undefined;
    setActiveItemId: React.Dispatch<React.SetStateAction<string | undefined>>;
};
export {};
